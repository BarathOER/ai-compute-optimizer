# Streamlit Cloud deploy branch -- SLIM requirements for dashboard.py only.
#
# The dashboard is a thin HTTP client for the gateway's /metrics and /query
# endpoints; it needs none of the heavy gateway stack (torch, sentence-
# transformers, chromadb, ...). The full gateway requirements live on `main`.
# Do NOT merge this file into main. Pins match main's requirements.txt exactly.
httpx==0.28.1
streamlit==1.41.1
pydantic==2.10.4
pydantic-settings==2.7.1
